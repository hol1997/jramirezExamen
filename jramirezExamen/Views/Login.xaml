<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="jramirezExamen.Resources.Login"
             Title="Login" Height="300" Width="400" >
    <VerticalStackLayout>
        <Grid>
            <Label Text="Login" FontSize="20"  Margin="0,20,0,0"/>

            <Grid Margin="20,60,20,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Text="Usuario:"  Margin="0,5"/>
                <Entry x:Name="usuarioEntry"  Margin="5" Grid.Row="0" Grid.Column="1"/>

                <Label Text="Contraseña:"  Margin="0,5" Grid.Row="1"/>
                <Entry x:Name="contrasenaEntry" Placeholder="Ingrese su contraseña" IsPassword="True"
                       Margin="5" Grid.Row="1" Grid.Column="1"/>

            </Grid>
        </Grid>
        <Button Text="Iniciar Seccion" Clicked="Button_Clicked"/>
    </VerticalStackLayout>
</ContentPage>